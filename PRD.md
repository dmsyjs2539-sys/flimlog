# [필름로그] PRD

## 1. 제품 개요

아날로그 카메라와 필름이 익숙하지 않은 초보자부터 기존 유저까지, 맞춤형 필름/카메라 추천 가이드부터 **지도 API 기반의 오프라인 자판기·현상소 위치 및 실시간 재고 조회**, 그리고 3~4단계의 간편한 현상/스캔 접수까지 한곳에서 해결할 수 있는 **통합 필름 웹 서비스**를 구축합니다.

## 2. 문제 정의

- **어려운 진입 장벽과 직관성 부족**: 사이트에 들어와도 무엇부터 시작해야 할지 모르고, 필름 종류, 사용법, 현상 접수 절차가 복잡하여 초보 사용자의 이탈률이 높음.
- **불편한 외부 지도/자판기 검색**: 24시간 자판기 위치나 현황을 확인하기 위해 외부 지도 서비스나 타 링크로 이탈해야 하며, 매장별/자판기별 실시간 재고를 미리 알 수 없어 불필요한 시간이 소요됨.
- **번거롭고 복잡한 현상 접수 절차**: 기존 12페이지에 달하는 구글폼 입력 방식으로 인해 접수 과정에서 피로감이 크고, 반복 재접수 시에도 매번 정보를 새로 입력해야 하는 불편함이 있음.

## 3. 목표 사용자

- **주요 사용자**:
  - **1순위 (입문자/20대 대학생)**: 레트로 감성을 좋아하지만 필름카메라 사용법이나 현상 절차를 전혀 모르는 초보자.
  - **2순위 (숙련자/20대 후반 직장인)**: 수동/일회용 필름카메라를 자주 사용하며 지도 기반 자판기 위치 검색 및 간편한 현상 접수를 원하는 기존 유저.
- **사용 환경**: 이동 중 모바일 웹 환경 (지도 기반 자판기 위치 검색, 간편 접수) 및 집/사무실 데스크톱 웹 환경 (가이드 탐색, 갤러리 감상).

## 4. 제품 목표

- 초보자가 가이드 및 직관적인 UI를 통해 1분 이내에 자신에게 맞는 필름/카메라를 추천받고 현상 접수까지 완료할 수 있도록 함.
- 외부 사이트 이탈 없이 **웹사이트 내 상단 지도 연동 영역에서 가까운 현상소/자판기 위치 마커를 확인**하고 실시간 재고(재고 있음, 품절임박, 재고 없음)를 미리 파악할 수 있도록 함.
- 기존의 복잡한 구글폼 접수를 대체하여 3~4단계의 원클릭/간편 현상 접수 프로세스를 제공함.

## 5. 제외 범위

- 이번 작업에서는 **실제 결제 PG사 연동**은 구현하지 않음 (접수 완료 단계까지 처리).
- 지도 API 연동 시 실제 지도 렌더링 및 핀 마킹, 커스텀 인포윈도우 조회를 수행하며, **백엔드 DB 연동 및 로그인 서버 인증**은 제외함 (브라우저 `localStorage` 및 JSON Mock 데이터 활용).

## 6. 디자인 기준

- 제공된 [Figma 디자인 파일](https://www.figma.com/design/lNUpIdgLj2eDnRZKQTS7oU/%EC%9C%A4%EC%9D%80%EC%84%A0?node-id=291-361&m=dev)을 시각 기준으로 사용합니다.
- 디자인에 없는 화면, 기능, 이미지, 아이콘을 임의로 추가하지 않습니다.
- 새 에셋을 만들기 전에 기존 `assets` 폴더와 디자인 원본을 확인합니다.
- 확인된 색상, 폰트, 간격, 라운드, 그림자를 우선 사용합니다.

## 7. 핵심 기능

- **필름/카메라 맞춤 추천 가이드**: 사용자의 취향과 촬영 스타일에 맞는 필름 및 입문용 카메라 추천 및 모범 사진 갤러리 제공.
- **지도 연동 자판기/현상소 위치 및 실시간 재고 조회**: Kakao/Naver 지도 API 연동을 통한 대화형 지도 UI 제공, 매장/자판기 위치 커스텀 마커 표시 및 실시간 재고 상태 노출.
- **3~4단계 간편 현상/스캔 접수**: 구글폼을 대체하는 직관적인 원클릭/간편 접수 Form 제공.
- **과거 접수 기록 불러오기 (원클릭 재접수)**: 기존 고객이 과거 접수한 데이터를 불러와 버튼 클릭 한 번으로 재접수 완료.

## 8. 화면 목록과 목적

### 인트로 / 스플래시 화면 (Intro)

- **목적**: 필름로그의 감성적인 브랜드 정체성을 전달하고 메인 서비스로의 자연스러운 진입을 유도.
- **주요 행동**: 브랜드 영상/비주얼 감상, `Skip` 버튼 클릭.
- **필요한 정보**: 브랜드 비주얼 에셋, 인트로 타이틀, Skip 버튼.
- **이동 경로**: 사이트 접속 시 최우선 노출 → 완료 후 또는 Skip 클릭 시 `메인 페이지`로 이동.

### 메인 페이지 (Main / Home)

- **목적**: 핵심 서비스(추천 가이드, 자판기 위치, 현상 접수) 안내 및 주요 메뉴로의 빠른 이동.
- **주요 행동**: GNB 메뉴 이동, CTA 버튼 클릭(현상/스캔 접수하기, 자판기 찾기), 스크롤 탐색.
- **필요한 정보**: Hero 비주얼/배너, 필름/카메라 추천 카드, 베스트 사진 갤러리, 지도 및 자판기 현황 요약 정보.
- **이동 경로**: `인트로`에서 진입 → GNB 및 CTA 버튼을 통해 `현상 접수`, `자판기 위치`, `마이페이지` 등으로 이동.

### 지도 연동 자판기 및 현상소 위치 안내 (Location / Map)

- **목적**: 지도 API를 연동하여 가까운 오프라인 필름 자판기 및 현상소의 상세 위치와 실시간 재고 상태 확인.
- **주요 행동**: 지도 확대/축소/이동, 매장/자판기 커스텀 마커 클릭, 상세 인포윈도우 레이어 확인, 지역 검색어 입력.
- **필요한 정보**: 지도 API 캔버스, 커스텀 마커 핀, 지도 인포윈도우, 자판기/매장 리스트 카드, 실시간 재고 상태(재고있음, 품절임박, 재고없음).
- **이동 경로**: `메인 페이지` GNB 또는 Quick Banner에서 진입.

### 간편 현상/스캔 접수 (Order / Reception)

- **목적**: 복잡한 구글폼 없이 3~4단계만으로 필름 현상 및 스캔 접수 완료.
- **주요 행동**: 접수 방식 선택(택배/방문/자판기 Drop), 필름 종류 및 수량 선택, 고객 정보 입력, 접수 완료 클릭.
- **필요한 정보**: 접수 단계 Indicator, 필름 옵션 선택 요소, 고객 정보 입력 폼, 과거 접수 기록 불러오기 버튼.
- **이동 경로**: `메인 페이지` CTA 버튼 및 `마이페이지` 재접수 버튼에서 진입.

## 9. 사용자 흐름

1. 사용자가 웹사이트에 접속하여 `인트로` 화면을 확인하거나 `Skip`을 누릅니다.
2. `메인 페이지`로 진입하여 맞춤 필름/카메라 추천 가이드를 둘러보거나 헤더 메뉴를 탐색합니다.
3. 가까운 자판기 및 현상소를 찾기 위해 `자판기/현상소 위치` 메뉴로 이동하여 **대화형 지도를 탐색하고 마커를 클릭해 실시간 재고를 확인**합니다.
4. `현상/스캔 접수하기` 버튼을 클릭하여 3~4단계의 간단한 입력만으로 접수를 진행합니다. (기존 유저는 과거 데이터 불러오기 활용)
5. 접수 완료 결과를 확인하고 마이페이지 또는 메인 화면으로 돌아갑니다.

## 10. 화면 상태

- **기본 상태**: 데이터 및 지도 API, 이미지가 정상적으로 로드된 초기 화면.
- **로딩 상태**: 지도 API 스크립트 로드, 위치 데이터 불러오기 및 접수 처리 시 스켈레톤(Skeleton Screen) 또는 로더(Spinner) 표시.
- **빈 상태**: 지도 내 자판기 검색 결과가 없거나 과거 현상 접수 내역이 없을 때 안내 문구 및 '검색 초기화/접수하러 가기' 버튼 제공.
- **오류 상태**: 지도 API 키/스크립트 로드 실패 시 에러 메시지 안내, 필수 입력값 누락 시 `has_error` 클래스 적용 및 붉은색 안내 텍스트 노출.
- **비활성 상태**: 필수 입력 항목이 미작성되었거나 재고가 없는 제품은 CTA 버튼 및 옵션 선택이 비활성화(`disabled`).

## 11. 데이터와 저장

- **데이터 출처**: 지도 API(Kakao/Naver Maps SDK) + 제공된 JSON 및 Mock 데이터 (`/assets/data/locations.json`, `/assets/data/products.json` 등).
- **브라우저 저장**: `localStorage` 사용.
  - key: `filmlog_recent_reception` (과거 현상 접수 기록 데이터 - 원클릭 재접수용)
  - key: `filmlog_user_settings` (초보자 가이드 선호도 설정값)
- **저장하지 않는 정보**: 비밀번호, 결제 카드 정보 등 민감한 개인정보.

## 12. 개발 조건

- HTML, CSS, JavaScript로 구현합니다.
- 지도 API(Kakao Maps API / Naver Maps API 등) 스크립트를 로드하여 대화형 지도를 연동합니다.
- React, Vue, TypeScript, Tailwind를 추가하지 않습니다.
- 기존 폴더 구조, 공통 스타일, 컴포넌트 역할을 우선 유지합니다.
- 제공된 에셋을 우선 사용합니다.
- 요청 없이 불필요한 추가 패키지나 외부 라이브러리를 추가하지 않습니다.

## 13. 명명 규칙

- CSS class와 HTML id는 snake_case를 사용합니다.
- 상태 class는 is_active, is_open, is_selected 형식으로 작성합니다.
- 오류 class는 has_error 형식으로 작성합니다.
- JavaScript 변수와 함수는 camelCase를 사용합니다.
- 불리언은 is, has, can, should로 시작합니다.
- 이벤트 함수는 handleXxx 형식으로 작성합니다.

## 14. 반응형 기준

- 모바일: 360px
- 태블릿: 768px
- 데스크톱: 1280px
- 모든 기준에서 페이지 전체 가로 스크롤이 없어야 합니다. (지도 영역 내 스크롤/드래그 동작과 전체 페이지 스크롤 간 터치 간섭 방지 처리)

## 15. 접근성

- 클릭 동작은 button, 페이지 이동은 a 요소를 사용합니다.
- 모든 입력은 label 또는 접근 가능한 이름을 가집니다.
- 키보드 focus-visible 상태가 보여야 합니다.
- 색상만으로 상태를 구분하지 않습니다. (지도 재고 상태 표시 시 색상 + 텍스트 병행 표시)
- 의미 있는 이미지는 alt를 가집니다.
- prefers-reduced-motion 설정을 반영합니다.

## 16. 인터랙션 라이브러리

- CSS와 기본 JavaScript로 가능한 동작은 별도 라이브러리 없이 구현합니다.
- 지도 연동은 Kakao Maps API 또는 Naver Maps API 연동 스크립트를 사용합니다.
- GSAP은 디자인에 복잡한 등장·전환 모션이 있을 때만 사용합니다.
- ScrollTrigger는 스크롤 위치와 연결된 모션에만 사용합니다.
- Lenis는 부드러운 스크롤이 제품 요구사항일 때만 사용합니다.
- Swiper는 실제 슬라이더나 갤러리에만 사용합니다.

## 17. 검증 방법

- 디자인 시안과 실제 브라우저 화면을 나란히 비교합니다.
- 지도 API 스크립트가 정상적으로 불러와지고 마커/인포윈도우 클릭 이벤트가 동작하는지 확인합니다.
- 360px, 768px, 1280px에서 잘림, 겹침, 가로 스크롤 및 지도 제어 동작을 확인합니다.
- 모든 버튼, 링크, 입력, 메뉴를 실제로 조작합니다.
- 키보드 Tab 순서와 focus-visible을 확인합니다.
- 콘솔 오류와 임시 console.log를 확인합니다.

## 18. 완료 조건

- 모든 화면과 상태가 PRD 및 디자인 시안과 일치합니다.
- 지도 연동 기능이 정상적으로 렌더링되고 마커 탐색이 가능합니다.
- 실제 에셋이 누락되지 않습니다.
- 주요 링크, 버튼, 입력, 인터랙션이 동작합니다.
- 반응형, 접근성, 콘솔 오류 검증을 통과합니다.
- 변경 파일, 구현 내용, 검증 결과, 확인하지 못한 부분이 보고됩니다.